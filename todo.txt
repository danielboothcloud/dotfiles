# Dotfiles reconciliation + zsh→fish migration
# Walk through one step at a time. Mark [x] as we go.
# todo.txt is in .chezmoiignore so it won't deploy to ~.

================================================================================
PHASE 1: Reconcile drift (keep zsh files for now)
================================================================================

[x] 1.  Fix dot_env.tmpl
        - Update GITHUB_ACCESS_TOKEN reference to:
          op://Homelab/dwhntxnsrngadsprlju47yp5z4/token
        - Verify `chezmoi execute-template < dot_env.tmpl` succeeds.

[~] 2.  SKIPPED — dot_aliases gets deleted in step 24 anyway. Fish aliases
        already moved to ~/.config/fish/conf.d/aliases.fish (interactive-only).
        Step 16 will pull both config.fish AND conf.d/aliases.fish into source.

[x] 3.  dot_config/private_atuin/private_config.toml — source already in
        desired state (uncommitted: opencode/deck → cursor in history_filter).
        Will be picked up by phase 1 commit.

[x] 4.  dot_gitignore_global — source matches home. Uncommitted edits
        (removed lefthook.yml/.crush, added _bmad-output/.cursor/.opencode)
        will be picked up by phase 1 commit.

[x] 5.  Brewfile updated via `packages/update`. Major cleanup along the way:
        untapped 6 orphan taps, dropped powershell cask (kept formula 7.6.1),
        removed php (cascade ~30 deps), oci-cli, hcloud, gomplate, kubeconform,
        infracost, mobile dev cluster, sidero-tools (kept talosctl), direnv,
        and others. Brewfile + gup.json refreshed.

[x] 6.  .chezmoiscripts/run_once_before_install-packages.sh.tmpl reconciled.
        Updated check from gup.conf → gup.json. Legacy gup.conf removed from
        repo. Added brew autoupdate start block (committed earlier).

[x] 7-8. dot_claude/ dropped entirely from chezmoi source. Decision: ~/.claude/
         is Claude Code runtime state, not dotfiles. Removed gsd hook
         references from ~/.claude/settings.json too.

[x] 9.  dot_config/nvim/init.lua reconciled. Source matches home — added
        OSC52 SSH clipboard block + terraform commentstring autocmd.

[x] 10. lazy-lock.json removed from chezmoi source — added to .chezmoiignore.
        Plugin lockfile now lives only in home, regenerated per-machine via
        :Lazy sync.

[x] 11. dot_config/nvim/lua/custom/plugins/telescope.lua reconciled.
        Full nvim tree verified: source ↔ home identical for all plugin
        files (active + disabled), excluding ignored lazy-lock.json + doc/.

[x] 12. zellij removed entirely — uninstalled from brew, dot_config/zellij/
        gone from source, ~/.config/zellij/ gone from home. The za/zd aliases
        from old zsh dot_aliases are correctly NOT migrated to fish.

[x] 13. yazi extras added to source via chezmoi add. Fixed oxocarbon flavor
        bug (duplicate fallback rules with old `name = "*"` syntax).
        rose-pine-moon flavor removed from home (unused).

[x] 14. Run `chezmoi diff` — should be empty.

[x] 15. COMMIT: phase 1 (reconcile drift)
        Suggested message: "chore: reconcile drifted home state into source"

================================================================================
PHASE 2: zsh + oh-my-zsh + p10k → fish + starship
================================================================================

[ ] 16. Add dot_config/fish/config.fish + dot_config/fish/conf.d/aliases.fish
        - config.fish: PATH (fish_add_path), env vars, mosh-bluefin block,
          starship/atuin/zoxide init, startup-time hook.
        - conf.d/aliases.fish: all aliases (status is-interactive guarded).

[ ] 17. Add dot_config/fish/fish_plugins
        - jorgebucaran/fisher
        - jhillyerd/plugin-git
        - oh-my-fish/plugin-bang-bang

[ ] 18. Add dot_config/fish/functions/ (custom only — fisher manages the rest)
        - kube.fish
        - clones.fish
        - ded.fish
        - fvi.fish

[ ] 19. Add dot_config/starship.toml

[ ] 20. Add .chezmoiscripts/run_once_after_install-fisher.sh
        - Bootstrap fisher if missing.
        - Run `fisher update` to install plugins from fish_plugins.

[ ] 21. Update packages/Brewfile
        - Add: brew "fish", brew "starship"
        - Remove: brew "powerlevel10k", brew "zsh-autosuggestions",
                  brew "zsh-syntax-highlighting"

[ ] 22. Remove dot_zshrc

[ ] 23. Remove dot_p10k.zsh

[ ] 24. Remove dot_aliases (zsh-shebanged; aliases moved to config.fish)

[ ] 25. Remove dot_config/zsh/ (kube/clones/ded already migrated)

[ ] 26. Remove .chezmoiscripts/run_once_before_install-oh-my-zsh.sh

[ ] 27. Update CLAUDE.md (both repo root and ~/projects/home/dotfiles/CLAUDE.md)
        - Replace zsh/omz/p10k references with fish/starship.
        - Update "Key Functions in .zshrc" section.
        - Document fisher plugin manager.

[ ] 28. Verify: `chezmoi diff` clean.
        Verify: open new fish shell, prompt renders, kube/clones/ded work.

[ ] 29. COMMIT: phase 2 (fish/starship migration)
        Suggested message: "feat: migrate zsh+omz+p10k to fish+starship"

================================================================================
PHASE 3: mise migration (replace pyenv + goenv)
================================================================================

[ ] 30. Install global tool versions via mise
        - mise use --global python@3.13
        - mise use --global go@latest
        - (Add others: node, ruby, etc. as needed)
        - Verify: mise ls

[ ] 31. Update dot_config/fish/config.fish
        - Remove: PYENV_ROOT, GOENV_ROOT env vars
        - Remove: fish_add_path entries for $HOME/.pyenv/bin, $HOME/.goenv/bin
        - Add: mise activate fish | source (inside `if status is-interactive`)

[ ] 32. Update packages/Brewfile
        - Remove: brew "pyenv", brew "goenv"
        - mise stays.

[ ] 33. Move .tool-versions / mise.toml into chezmoi if you want global
        version pins committed. (Optional — skip if you prefer per-project.)

[ ] 34. Cleanup: brew uninstall pyenv goenv
        Optionally rm -rf ~/.pyenv ~/.goenv (downloaded toolchains)

[ ] 35. Verify: open new fish, `python --version` and `go version` resolve
        through mise shims.

[ ] 36. COMMIT: phase 3 (mise migration)
        Suggested message: "feat: migrate pyenv+goenv to mise"

================================================================================
OPEN QUESTIONS (answer when relevant — not blocking)
================================================================================

Q1. fish_variables: skip from chezmoi (regenerated runtime state)? — leaning YES
Q2. completions/remotex.fish: skip (auto-generated by remotex)? — leaning YES
Q3. mosh auto-connect to bluefin-vm in config.fish: keep as general behavior? — needs answer
Q4. ~/.env has literal ghp_…WKu24 token — is it still active or should we
    rotate before committing the new op:// reference works?
